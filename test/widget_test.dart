// Stage 0 — placeholder test.
// The default Flutter widget test references a class that no longer exists.
// Real tests begin in Stage 1.

import 'package:flutter_test/flutter_test.dart';

void main() {
  test('Stage 0 placeholder', () {
    expect(1 + 1, 2);
  });
}
